import { Link } from "react-router-dom";
const NotLog = () => {
  return (
    <div className="w-full">
      <Link to="/userdashboard/" className="text-center">
        DashBoard
      </Link>
    </div>
  );
};

export default NotLog;
